[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
startCommand = "openfang start --config config.toml"
healthcheckPath = "/"
healthcheckTimeout = 300
