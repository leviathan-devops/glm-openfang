[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
startCommand = "PORT=${PORT:-8080} OPENFANG_HOME=/data ./target/release/openfang start"
healthcheckPath = "/"
healthcheckTimeout = 300
