[build]
builder = "nixpacks"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
startCommand = "mkdir -p /data && openfang start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
